# Patrol: gastowndocs/refinery · 2026-04-06T14:25

**Trigger:** Deacon patrol (gastowndocs/refinery <- deacon • 2026-04-06T14:25 • patrol)

## Summary

| Step | Status |
|------|--------|
| gt hook | ✓ **ga-8tzjwn** (HANDOFF: delegation complete, infrastructure blocked; **no molecule** → inline patrol). Note: first `gt hook` attempt hit transient MySQL `unexpected EOF`; `gt hook status` / `bd ready` succeeded shortly after. |
| gt mq list gastowndocs | ✗ Dolt **ChunkStore** panic (`empty chunk returned from ChunkStore`) on `bd list` |
| gt refinery ready gastowndocs | ✗ same ChunkStore panic when listing merge-requests |
| bd sync --import-only (rig) | ✗ JSONL import `bufio.Scanner: token too long` |
| gt mail inbox gastowndocs/refinery | ✓ 0 messages |
| gt refinery status gastowndocs | ✓ ● RUNNING, queue 0, session `gt-gastowndocs-refinery` |
| gt dolt status | ✓ server running; **warnings:** query latency ~950ms, connections **27/50 (54%)** |
| gt deacon status | ✓ ● RUNNING detached (session 2026-04-06 **14:25:26**, aligned with trigger) |
| npm run build (rig) | ✓ ~32s; blog author warnings only; `static/api/docs.json` **reverted** |

## Observations

- **Hook:** Same as prior patrols: Mayor HANDOFF on **ga-8tzjwn** with no molecule — refinery-scope checks run inline.
- **Beads / Dolt:** Town `beads` store still panics on indexed queries (ChunkStore); rig JSONL import still blocked by oversized line.
- **Refinery & Deacon:** Running; queue empty; mail empty.
- **Dolt:** Connection use improved vs 13:33 patrol (54% vs 78%); query latency still high (~950ms).

## Next patrol

- Recover **beads** Dolt store (ChunkStore) and JSONL line length / `bd sync --import-only` per town P0 runbooks (**gm-y2nde5**, **ga-4chxe9**, etc.).
- Retry **`bd mol burn ga-wisp-ibj`** (or close steps manually) when `bd` is stable.
- Re-check **`gt mq list`** / **`gt refinery ready`** after beads repair.

**PATROL COMPLETE** — refinery / deacon / rig build OK; **MQ, ready MR listing, and rig JSONL import** still degraded.
