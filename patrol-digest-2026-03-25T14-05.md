**Digest:** Patrol executed (gastowndocs/refinery). **Hook** — ga-8tzjwn (Mayor HANDOFF “Delegation complete, infrastructure blocked”; **closed**; no molecule). Treated as empty for refinery scope; **mol-refinery-patrol** wisp **CREATED** — **ga-wisp-nuoc9** (vapor, 12 children). `bd mol burn ga-wisp-nuoc9` **hung** (aborted); follow up after `bd` import healthy. **Refinery** gastowndocs ● RUNNING, queue 0, session `gt-gastowndocs-refinery`. **MQ** empty (`gt mq list gastowndocs`). **Deacon** was ○; **started** → ● RUNNING detached (2026-03-25 14:06:08). **gt dolt status** — Dolt server not running. **npm run build** ✓ (~14s; blog author warnings); `static/api/docs.json` reverted. **gt mail inbox gastowndocs/refinery** ✓ (0 unread). **bd sync** ✗ import JSONL `token too long`. **Cleanup** — `bd list --label-any cleanup --status=open --allow-stale`: **6** open P2 polecat zombie rows. **Polecats** — `timeout 25 gt polecat list gastowndocs` → 124. Rig **build OPERATIONAL**; **bd import** and **polecat survey** still degraded.

**Next session:** Repair JSONL / `bd sync --import-only` (oversized line); retry `bd mol burn ga-wisp-nuoc9` or run mol children; check `gt hook`; start Deacon if ○; triage cleanup polecat zombies when owned.
